% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scaffold.R
\name{create_project}
\alias{create_project}
\title{Scaffold a new research project}
\usage{
create_project(
  name,
  path = ".",
  type = c("analysis", "package"),
  targets = FALSE,
  git = TRUE
)
}
\arguments{
\item{name}{Project name (also the new directory name).}

\item{path}{Parent directory in which to create the project (default ".").}

\item{type}{\code{"analysis"} or \code{"package"}.}

\item{targets}{Logical; for analysis projects, scaffold a \verb{_targets.R} stub.}

\item{git}{Logical; initialise a fresh git repository (default \code{TRUE}).}
}
\value{
The created project path, invisibly.
}
\description{
Creates a project of one of two archetypes:
\itemize{
\item \code{"analysis"} - builds the \verb{code/} + \verb{data/} + \verb{output/} skeleton via
\code{\link[=make_file_dirs]{make_file_dirs()}} and writes the project mechanics directly (a PHI-aware
\code{.gitignore}, an \code{renv}-activation \code{.Rprofile}, \verb{<name>.Rproj}, a README
stub, the \verb{memos/} + \code{workflow-feedback.md} state stack, and an optional
\code{targets} pipeline stub). It writes \strong{no} \code{CLAUDE.md} and \strong{no} \verb{.claude/}:
the \code{project-setup} skill (biostat plugin) writes both the router and the
traveling \verb{.claude/hooks/} guard set, copied from the plugin's own canonical
copy (\verb{plugins/biostat/hooks/}) — not from an installed kernel. PHI
protection that must travel with the repo regardless lives in the
\code{.gitignore} (raw/derived data and data extensions are ignored).
\item \code{"package"} - wraps \code{\link[usethis:create_package]{usethis::create_package()}} and layers the package
best-practices set (MIT license, testthat 3, roxygen markdown, pkgdown,
R-CMD-check + coverage + pkgdown GitHub Actions, NEWS, self-contained
\verb{.claude/}).
}
}
\seealso{
The \code{project-setup} skill (biostat marketplace), which calls this to
scaffold mechanics and then generates the project router.
}
